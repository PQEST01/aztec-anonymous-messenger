/// <reference types="node" resolution-mode="require"/>
import { AliasHash } from '@aztec/barretenberg/account_id';
import { GrumpkinAddress } from '@aztec/barretenberg/address';
import { AssetValue } from '@aztec/barretenberg/asset';
import { ProofId } from '@aztec/barretenberg/client_proofs';
import { TxId } from '@aztec/barretenberg/tx_id';
export declare class UserAccountTx {
    readonly txId: TxId;
    readonly userId: GrumpkinAddress;
    readonly aliasHash: AliasHash;
    readonly newSpendingPublicKey1: Buffer | undefined;
    readonly newSpendingPublicKey2: Buffer | undefined;
    readonly migrated: boolean;
    readonly fee: AssetValue;
    readonly created: Date;
    readonly settled?: Date | undefined;
    readonly proofId = ProofId.ACCOUNT;
    constructor(txId: TxId, userId: GrumpkinAddress, aliasHash: AliasHash, newSpendingPublicKey1: Buffer | undefined, newSpendingPublicKey2: Buffer | undefined, migrated: boolean, fee: AssetValue, created: Date, settled?: Date | undefined);
}
//# sourceMappingURL=user_account_tx.d.ts.map