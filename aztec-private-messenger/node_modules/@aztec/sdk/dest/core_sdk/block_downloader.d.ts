import { RollupProvider } from '@aztec/barretenberg/rollup_provider';
import { Block } from '@aztec/barretenberg/block_source';
export declare class BlockDownloader {
    private rollupProvider;
    private runningPromise?;
    private running;
    private from;
    private interruptableSleep;
    private semaphore;
    private queue;
    private genesisTake;
    private debug;
    constructor(rollupProvider: RollupProvider, maxQueueSize: number, initialTreeSize: number);
    start(from?: number): void;
    stop(): Promise<void>;
    getBlocks(): Promise<Block[]>;
}
//# sourceMappingURL=block_downloader.d.ts.map