/// <reference types="node" resolution-mode="require"/>
import { GrumpkinAddress } from '@aztec/barretenberg/address';
import { AccountData } from '@aztec/barretenberg/environment';
import { Hasher } from '@aztec/barretenberg/merkle_tree';
import { Alias, SpendingKey } from '../database/database.js';
export interface GenesisData {
    aliases: Alias[];
    spendingKeys: SpendingKey[];
    subTreeRoots: Buffer[];
}
export declare function parseGenesisAliasesAndKeys(accounts: AccountData[]): {
    aliases: Alias[];
    spendingKeys: SpendingKey[];
};
export declare function getUserSpendingKeysFromGenesisData(userPublicKeys: GrumpkinAddress[], accountsData: Buffer, hasher: Hasher, rollupSize: number): Promise<SpendingKey[]>;
//# sourceMappingURL=index.d.ts.map