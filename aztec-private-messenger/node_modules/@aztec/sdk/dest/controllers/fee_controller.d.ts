import { GrumpkinAddress } from '@aztec/barretenberg/address';
import { AssetValue } from '@aztec/barretenberg/asset';
import { Tx } from '@aztec/barretenberg/rollup_provider';
import { TxId } from '@aztec/barretenberg/tx_id';
import { CoreSdk } from '../core_sdk/index.js';
import { Signer } from '../signer/index.js';
export declare class FeeController {
    readonly userId: GrumpkinAddress;
    private readonly userSigner;
    readonly proofTxs: Tx[];
    readonly fee: AssetValue;
    private readonly core;
    private feeProofOutputs;
    private txIds;
    constructor(userId: GrumpkinAddress, userSigner: Signer, proofTxs: Tx[], fee: AssetValue, core: CoreSdk);
    createProof(timeout?: number): Promise<void>;
    exportProofTxs(): Tx[];
    send(): Promise<TxId>;
    getTxIds(): TxId[];
    awaitSettlement(timeout?: number): Promise<void>;
}
//# sourceMappingURL=fee_controller.d.ts.map