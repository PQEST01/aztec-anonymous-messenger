/// <reference types="node" resolution-mode="require"/>
import { SchnorrSignature } from '../../crypto/index.js';
import { UnrolledProver } from '../prover/index.js';
import { AccountTx } from './account_tx.js';
export declare class AccountProver {
    private prover;
    readonly mock: boolean;
    constructor(prover: UnrolledProver, mock?: boolean);
    static getCircuitSize(proverless?: boolean): number;
    computeKey(timeout?: number): Promise<void>;
    releaseKey(): Promise<void>;
    loadKey(keyBuf: Buffer): Promise<void>;
    getKey(): Promise<Buffer>;
    computeSigningData(tx: AccountTx): Promise<Buffer>;
    createAccountProof(tx: AccountTx, signature: SchnorrSignature, timeout?: number): Promise<Buffer>;
    getProver(): UnrolledProver;
}
//# sourceMappingURL=account_prover.d.ts.map